<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="schedule.sumdu.edu.ua.schedulesumdu">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>

    <application
        android:allowBackup="true"
        android:label="@string/app_name"
        android:icon="@drawable/ic_launcher"
        android:name=".ScheduleSumduApp"
        android:theme="@style/Theme.AppCompat.Light">

        <activity
            android:name="schedule.sumdu.edu.ua.schedulesumdu.MainActivity"
            android:screenOrientation="portrait"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name="schedule.sumdu.edu.ua.schedulesumdu.ChangeDateActivity"
            android:screenOrientation="portrait"
            android:label="@string/app_name" >
        </activity>
        <activity
            android:name="schedule.sumdu.edu.ua.schedulesumdu.ChangeListActivity"
            android:screenOrientation="portrait"
            android:label="@string/app_name">
        </activity>
        <activity
            android:name="schedule.sumdu.edu.ua.schedulesumdu.LookScheduleActivity"
            android:screenOrientation="portrait"
            android:label="@string/app_name">
        </activity>
    </application>

</manifest>
